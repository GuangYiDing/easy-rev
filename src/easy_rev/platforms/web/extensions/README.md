# Web extensions

The shipping Chrome extension lives at the repo root:

`extensions/easy-rev-chrome/`

This package path is reserved so packaging can later vendor extension assets
next to the Python web engine. Prefer the root `extensions/` tree for development.
